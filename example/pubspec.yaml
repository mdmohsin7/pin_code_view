name: pin_code_view_example
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <= 3.6.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  pin_code_view: 
    path: ../

flutter:
  uses-material-design: true